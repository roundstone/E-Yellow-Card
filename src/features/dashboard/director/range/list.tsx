import DirectorRangeList from "@/components/pages/director/dashboard/range";
import React from "react";

type Props = {};

const DirectorRangeListPage = (props: Props) => {
  return <DirectorRangeList />;
};

export default DirectorRangeListPage;
