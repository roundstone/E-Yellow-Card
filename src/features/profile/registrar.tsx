import SharedProfile from "@/components/pages/profile";
import React from "react";

type Props = {};

const SharedProfilePage = (props: Props) => {
  return <SharedProfile />;
};

export default SharedProfilePage;
